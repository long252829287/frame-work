# 项目样式指南与开发规范

本文档概述了应用程序的样式架构、设计令牌（Design Tokens）和开发模式。本项目采用 **CSS 变量优先（CSS Variable-first）** 的方法，并利用 SCSS 进行结构组织和 Mixin 复用。

## 1. 目录结构

样式逻辑位于 `src/assets/scss`：

```
src/assets/scss/
├── global.scss               # 主入口文件（重置、Element Plus 覆盖、原子类）
└── themes/
    └── modern-minimal.scss   # CSS 变量定义（设计令牌）& SCSS Mixins
```

> **经验法则**：
>
> - **变量**：在 `modern-minimal.scss` 中定义**所有**设计令牌（颜色、间距等）。
> - **组件**：在 Vue 组件的 `<style>` 或全局覆盖中使用这些变量。
> - **Mixins**：对于复杂的交互元素（按钮、输入框、卡片），使用共享的 Mixins。

---

## 2. 设计令牌 (CSS 变量)

我们使用语义化命名变量，以解耦设计与实现。

### 🎨 颜色系统

基于 **Slate**（中性色）和 **Indigo/Violet**（强调色）色阶。

#### 背景色 (Backgrounds)

| 变量名                 | 值        | 用途                                         |
| :--------------------- | :-------- | :------------------------------------------- |
| `--color-bg-primary`   | White     | 主要内容区域、输入框背景                     |
| `--color-bg-secondary` | Slate 50  | 页面背景、较浅的悬停状态                     |
| `--color-bg-tertiary`  | Slate 100 | 次级容器、代码块                             |
| `--color-bg-elevated`  | White     | 卡片、模态框、气泡卡片（同为白色但语义不同） |

#### 文本颜色 (Text)

| 变量名                   | 值        | 用途                     |
| :----------------------- | :-------- | :----------------------- |
| `--color-text-primary`   | Slate 900 | 标题、正文主要文字       |
| `--color-text-secondary` | Slate 600 | 元数据、辅助文字、描述   |
| `--color-text-tertiary`  | Slate 400 | 占位符、图标             |
| `--color-text-disabled`  | Slate 300 | 禁用状态                 |
| `--color-text-inverse`   | White     | 主按钮或深色背景上的文字 |

#### 边框颜色 (Borders)

| 变量名                     | 值         | 用途                           |
| :------------------------- | :--------- | :----------------------------- |
| `--color-border-primary`   | Slate 200  | 强边框（输入框、卡片、分割线） |
| `--color-border-secondary` | Slate 100  | 弱边框（表格行）               |
| `--color-border-focus`     | Indigo 500 | 聚焦环                         |

#### 品牌强调色 (Brand Accents)

| 变量名                         | 值         | 用途                                   |
| :----------------------------- | :--------- | :------------------------------------- |
| `--color-accent-primary`       | Indigo 600 | **主要操作色**（按钮、链接、激活状态） |
| `--color-accent-primary-hover` | Indigo 700 | 悬停状态                               |
| `--color-accent-primary-light` | Indigo 100 | 激活背景、选中高亮                     |
| `--color-accent-secondary`     | Violet 600 | 次要操作、装饰元素                     |

#### 语义化颜色 (Semantic Colors)

| 状态               | 变量名            | 用途               |
| :----------------- | :---------------- | :----------------- |
| **成功 (Success)** | `--color-success` | 成功消息、徽章     |
| **警告 (Warning)** | `--color-warning` | 警示、需要注意     |
| **错误 (Error)**   | `--color-error`   | 验证错误、删除操作 |
| **信息 (Info)**    | `--color-info`    | 信息提示           |

---

### 📐 间距系统 (Spacing)

使用这些变量来设置 `margin`、`padding` 和布局间隙。

| 变量名          | 尺寸    | 像素 |
| :-------------- | :------ | :--- |
| `--spacing-xs`  | 0.25rem | 4px  |
| `--spacing-sm`  | 0.5rem  | 8px  |
| `--spacing-md`  | 1rem    | 16px |
| `--spacing-lg`  | 1.5rem  | 24px |
| `--spacing-xl`  | 2rem    | 32px |
| `--spacing-2xl` | 3rem    | 48px |
| `--spacing-3xl` | 4rem    | 64px |

### 🌑 阴影系统 (Shadows)

我们使用 **彩色阴影**（蓝灰色调）以获得更干净、更高级的外观。

| 变量名        | 用途                       |
| :------------ | :------------------------- |
| `--shadow-xs` | 极细微深度（边框效果）     |
| `--shadow-sm` | 小卡片、按钮               |
| `--shadow-md` | **默认卡片阴影**、下拉菜单 |
| `--shadow-lg` | 模态框、推荐项             |
| `--shadow-xl` | 大型遮罩层                 |

---

## 3. SCSS Mixins

在任何 `.scss` 或 `.vue` 文件中导入主题文件以使用这些 mixin：

```scss
@use '@/assets/scss/themes/modern-minimal.scss' as theme;

.my-component {
  @include theme.card;
}
```

### `@mixin card`

创建一个标准容器，包含：

- 抬升的背景色
- 浅色边框
- 基础阴影
- 悬停效果（轻微上浮 + 加深阴影）

### `@mixin button-primary`

标准主要操作按钮：

- 背景：强调色
- 文字：白色
- 包含悬停/激活（Hover/Active）状态
- 包含聚焦环

### `@mixin button-secondary`

幽灵/描边按钮：

- 背景：透明/白色
- 边框：浅色
- 悬停：灰色背景 + 强调色边框/文字

### `@mixin input`

标准表单输入框样式：-带边框

- 聚焦时过渡动画
- 占位符颜色
- 禁用状态样式

---

## 4. 开发指南

### 如何编写组件样式

1.  **首选全局类/组件**：
    - 使用 `<el-button type="primary">` 而不是样式化一个 `div`。
    - 使用 `<el-card>` 作为容器。

2.  **在 Vue 中使用变量**：

    ```vue
    <style scoped>
    .custom-box {
      /* 推荐：语义化用法 */
      background-color: var(--color-bg-primary);
      border: 1px solid var(--color-border-primary);
      color: var(--color-text-primary);
      padding: var(--spacing-md);
    }

    .bad-box {
      /*哪怕：硬编码值 */
      background-color: #fff;
      border: 1px solid #e2e8f0;
    }
    </style>
    ```

3.  **覆盖 Element Plus**：
    主题文件会自动覆盖 Element Plus 变量。如果你需要特定覆盖，请在 `global.scss` 中定义，或局部使用 `var(--color-...)` 令牌。

4.  **暗黑模式兼容 (Dark Mode)**：
    - 始终使用 `var(--color-...)` 令牌。
    - **绝不** 在具体组件中硬编码十六进制值（如 `#fff`, `#000`），否则实施暗黑模式时会失效（相关定义已在 `modern-minimal.scss` 中预设）。

### 添加新颜色/令牌

1.  打开 `src/assets/scss/themes/modern-minimal.scss`。
2.  在相应部分下添加变量定义。
3.  如果需要，在 `@media (prefers-color-scheme: dark)` 块中添加其暗黑模式等效值。

## 5. Tailwind 集成 (如果使用)

如果使用 Tailwind，请在 `tailwind.config.js` 中映射这些变量以扩展主题：

```javascript
theme: {
  extend: {
    colors: {
      primary: 'var(--color-accent-primary)',
      bg: {
        primary: 'var(--color-bg-primary)',
        secondary: 'var(--color-bg-secondary)',
      },
      // ...
    }
  }
}
```
