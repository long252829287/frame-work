# 这是顶层配置文件
root = true

[*]
# 编码格式
charset = utf-8
# 缩进风格：space (空格) 或 tab (制表符)
indent_style = space
# 缩进大小：设置为 2
indent_size = 2
# Tab 键的宽度：也设置为 2
tab_width = 2
# 换行符类型
end_of_line = lf
# 在文件末尾插入一个空行
insert_final_newline = true
# 删除行尾多余的空格
trim_trailing_whitespace = true